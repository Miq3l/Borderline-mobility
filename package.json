{
  "name": "n8n-browserless-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Scripts executed by n8n via SSH/docker exec",
  "type": "module",
  "main": "browserlessTest.js",
  "scripts": {
    "deploy": "./deploy-n8n.sh",
    "deploy:fast": "./deploy-n8n.sh --skip-install",
    "run:mobility": "node scripts/mobilityLoginStart.js",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix"
  },
  "devDependencies": {
    "oxlint": "1.59.0"
  },
  "dependencies": {
    "playwright-core": "^1.59.0",
    "zod": "^4.3.6"
  }
}
